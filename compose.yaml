services:
  daybreak:
    build:
      context: .
      dockerfile: Dockerfile
    image: daybreak:local
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - daybreak-data:/data

volumes:
  daybreak-data:
